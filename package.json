{
  "name": "alipsidikp.github.io",
  "version": "1.0.0",
  "description": "Tech Blog using GitHub Pages",
  "scripts": {
    "generate": "node scripts/generate-local.js",
    "start": "npm run generate && npx http-server . -p 3000"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^4.3.0",
    "reading-time": "^1.5.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}